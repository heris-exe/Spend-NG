# Supabase (required for cloud sync across devices)
# Get these from: https://app.supabase.com → your project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# App URL used for OAuth redirect after Google sign-in.
# Dev:  VITE_APP_URL=http://localhost:5173
# Prod: VITE_APP_URL=https://your-app.vercel.app  (must match Supabase Site URL)
# If OAuth redirects to localhost:3000, set Supabase Site URL to your production URL.
# VITE_APP_URL=https://spendng.vercel.app
